const theme = {
  fontFamily: 'Arial, Helvetica, sans-serif',
  primaryColor: '#8E0B56',
  secondaryColor: '#242D4A',
  textColor: '#1F1A1A'
};

export type ITheme = typeof theme;
export default theme;
